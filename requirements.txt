# Core LLM optimization framework
textgrad>=0.1.6

# Hugging Face dataset loading & API
datasets>=2.14.0
huggingface_hub>=0.20.0

# Local embedding model for retrieval (all-MiniLM-L6-v2)
sentence-transformers>=2.2.2
torch>=2.0.0
transformers>=4.36.0

# Numerical / data processing
numpy>=1.24.0

# LLM provider client (DeepSeek uses OpenAI-compatible API)
openai>=1.10.0

# Optional but recommended: progress bars and tokenizer speedup
tqdm>=4.65.0
